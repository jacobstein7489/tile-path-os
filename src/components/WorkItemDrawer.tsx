/**
 * The old bespoke drawer is retired. Every screen now renders the single
 * canonical work item panel, so Today, Work and project work share one
 * interaction model and one Move Forward flow.
 */
export { WorkItemPanel as WorkItemDrawer } from "@/components/work/WorkItemPanel";
